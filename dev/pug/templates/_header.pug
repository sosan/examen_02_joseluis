header.header
    nav.header__header-nav
        label(class="header-nav__label", for="click")
            img(src="./assets/img/svg/menu_hamburguesa.svg", alt="icono menu hamburguesa")
        input(class="header-nav__input", type="checkbox", name="click", id="click")
        ul.header-nav__ul-header
            li.ul-header__menu__index
                if currentpage == "index"
                    a(class="header-nav__inicio", href="#")
                        | Inicio
                else
                    a(class="header-nav__inicio", href="index.html")
                        | Inicio
            li.ul-header__menu__proyectos
                if currentpage == "proyectos"
                    a(class="header-nav__proyectos", href="#")
                        | Proyectos
                else
                    a(class="header-nav__proyectos", href="proyectos.html")
                        | Proyectos
            li.ul-header__menu__quienes
                if currentpage == "quienes"
                    a(class="header-nav__quienes", href="#")
                        | Quienes somos
                else
                    a(class="header-nav__quienes", href="quienes.html")
                        | Quienes somos
            
            li.ul-header__menu__contactar
                if currentpage == "contactar"
                    a(class="header-nav__contactar", href="#")
                        | Contactar
                else
                    a(class="header-nav__contactar", href="contactar.html")
                        | Contactar